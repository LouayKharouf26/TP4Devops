stages:
  - build_joular
  - build_and_run

# Stage to clone and build the JoularJX agent
build_joular:
  stage: build_joular
  image: maven:3.8.1-jdk-11
  script:
    - echo "Cloning JoularJX repository..."
    - git clone https://github.com/joular/joularjx.git
    - cd joularjx/
    - echo "Building JoularJX agent..."
    - mvn clean install -DskipTests
  artifacts:
    paths:
      - joularjx/target/joularjx.jar
    expire_in: 1 week

# Stage to build your application and run it with the JoularJX agent
build_and_run:
  stage: build_and_run
  image: maven:3.8.1-jdk-11
  dependencies:
    - build_joular
  script:
    - echo "Building the application..."
    - mvn clean package
    - echo "Running the application with JoularJX agent..."
    - java -javaagent:/root/.m2/repository/org/noureddine/joularjx/3.0.1/joularjx-3.0.1.jar -jar target/tp4-0.0.1-SNAPSHOT.jar
